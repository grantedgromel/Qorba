// Generated types live in openapi.d.ts. Re-export the convenient `paths` and
// `components` namespaces so consumers can write `import type { paths } from
// "@qorba/shared"`.
export type { paths, components, operations } from "./openapi";

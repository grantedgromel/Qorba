import { ComingSoon } from "@/components/layout/ComingSoon";

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Calendar."
      sprint="SPRINT 3"
      description="Year-by-month heatmap with magnitude-scaled tinting, calendar-year totals with YTD, and best/worst quarter callouts."
    />
  );
}

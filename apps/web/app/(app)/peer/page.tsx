import { ComingSoon } from "@/components/layout/ComingSoon";

export default function PeerPage() {
  return (
    <ComingSoon
      title="Peer group."
      sprint="SPRINT 4"
      description="Quartile fan vs your peer set, rolling percentile rank, and the constellation view — risk-return scatter with the fund pinned."
    />
  );
}

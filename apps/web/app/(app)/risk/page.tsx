import { ComingSoon } from "@/components/layout/ComingSoon";

export default function RiskPage() {
  return (
    <ComingSoon
      title="Risk measures."
      sprint="SPRINT 3"
      description="Volatility, downside deviation, semi-deviation, VaR/CVaR, skew and kurtosis. Each pinned to the chip bar via Cmd-K."
    />
  );
}

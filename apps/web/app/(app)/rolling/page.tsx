import { ComingSoon } from "@/components/layout/ComingSoon";

export default function RollingPage() {
  return (
    <ComingSoon
      title="Rolling analytics."
      sprint="SPRINT 3"
      description="Rolling Sharpe, vol, beta, and correlation across configurable windows — 12, 24, 36, 60 months, layered on the same chart."
    />
  );
}

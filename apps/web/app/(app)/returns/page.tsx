import { ComingSoon } from "@/components/layout/ComingSoon";

export default function ReturnsPage() {
  return (
    <ComingSoon
      title="Return measures."
      sprint="SPRINT 3"
      description="CAGR, arithmetic and geometric annualized returns, period returns, and the calendar-year breakdown — all driven by the metric palette and the time-window picker."
    />
  );
}

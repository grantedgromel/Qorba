import { ComingSoon } from "@/components/layout/ComingSoon";

export default function RegressionPage() {
  return (
    <ComingSoon
      title="Regression."
      sprint="SPRINT 3"
      description="Single-factor CAPM versus any benchmark in the library, with alpha, beta, t-stat, R², tracking error, and a clean scatter."
    />
  );
}
